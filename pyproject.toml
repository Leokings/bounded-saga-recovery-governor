[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
markers = [
  "integration: requires a running GenLayer network",
  "hosted: opt-in real-inference test for StudioNet or Bradbury",
]
norecursedirs = [".venv", "node_modules", ".artifacts"]
